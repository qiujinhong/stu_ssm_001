<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.codejams.mapper.BookMapper">

    <select id="findAllBook" resultType="book">
        select * from book
    </select>

    <select id="findBookById" resultType="book" parameterType="int">
        select * from book where id = #{id}
    </select>

    <update id="update" parameterType="book">
        update book set name = #{name}, author = #{author}, detail = #{detail} where id = #{id}
    </update>

    <delete id="delete" parameterType="int">
        delete from book where id = #{id}
    </delete>

    <insert id="save" parameterType="book">
        insert into book values(null,#{name},#{author},#{detail},"空闲")
    </insert>

    <select id="findBookByCondition" parameterType="string" resultType="book">
        SELECT * from book where name like CONCAT(CONCAT('%',#{condition},'%'))
            or id like CONCAT(CONCAT('%',#{condition},'%'))
            or author like CONCAT(CONCAT('%',#{condition},'%'))
            or detail like CONCAT(CONCAT('%',#{condition},'%'))
            or status like CONCAT(CONCAT('%',#{condition},'%'))
    </select>
    
    <update id="changeStatusToLeisure" parameterType="string">
        update book set status = "空闲" where name = #{name}
    </update>

    <update id="changeStatusToNotLeisure" parameterType="string">
        update book set status = "已借出" where name = #{name}
    </update>

</mapper>